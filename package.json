{
  "name": "npos",
  "version": "0.1.0",
  "description": "Neurofunk Production OS — unified monorepo root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "node server/api.js",
    "dev:web": "npm --prefix web run dev",
    "start": "node server/api.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
